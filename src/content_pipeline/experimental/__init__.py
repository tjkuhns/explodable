"""Experimental pipeline modules.

These modules are used by `hybrid_graph.py` for measurement runs and
ablation studies. Production content generation uses
`src/content_pipeline/graph.py` directly and does not import from this
package.

See README.md in this directory for the production/experimental
boundary and the criteria for promoting a module into the main
pipeline.
"""
